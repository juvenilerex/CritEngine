This file details the rules for styling of the engine's source code.

Whenever accessing a method or member within the same class, usage of 'this->' must be explicit, this is to avoid confusion between member variables and local variables.